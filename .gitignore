.build/
build/
.DS_Store
*.xcodeproj
*.xcworkspace
.swiftpm/
.idea/

# Личный контекст форка — в публичный репозиторий ему не место
.paiw/

# Инструкции openspec в .claude/ коммитятся, а локально одобренные разрешения
# машинно-специфичны
.claude/settings.local.json

# Чтобы секрет не уехал в публичный репозиторий по случайности
.env
.env.*
*.pem
*.p12
*.key
*.mobileprovision
*.certSigningRequest
